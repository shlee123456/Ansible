---
# llm 그룹 변수 (LLM 서빙 서버)

# 모델 저장 표준 경로 (전용 마운트 전제 — 역할이 마운트/용량을 사전 체크)
llm_models_dir: /media/llm-models

# 다운로드할 모델 목록. dest 미지정 시 {{ llm_models_dir }}/<repo 이름>
llm_models:
  - repo_id: puzzleaihf/gpt-oss-120b-260109

# HuggingFace 토큰 (vault 암호화 변수 참조)
llm_hf_token: "{{ vault_hf_token }}"
