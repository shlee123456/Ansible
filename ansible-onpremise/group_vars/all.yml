---
# 공통 변수 설정
system_packages:
  - vim
  - git
  - curl
  - htop
  - tree
  - net-tools
  - unzip
  - wget

# 한글 로케일 설정
locale: ko_KR.UTF-8
timezone: Asia/Seoul

# Docker 설정 (docker role에서 사용)
docker_user: "{{ ansible_user }}"
